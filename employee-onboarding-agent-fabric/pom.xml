<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

	<modelVersion>4.0.0</modelVersion>
	<groupId>com.mulesoft.mcp</groupId>
	<artifactId>employee-onboarding-mcp-parent</artifactId>
	<version>1.0.0</version>
	<packaging>pom</packaging>

	<name>Employee Onboarding MCP Parent</name>
	<description>Parent POM for Employee Onboarding MCP Servers</description>

	<modules>
		<module>mcp-servers/employee-onboarding-mcp</module>
		<module>mcp-servers/asset-allocation-mcp</module>
		<module>mcp-servers/notification-mcp</module>
		<module>mcp-servers/agent-broker-mcp</module>
	</modules>

	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
		<maven.compiler.source>17</maven.compiler.source>
		<maven.compiler.target>17</maven.compiler.target>
		<mule.version>4.4.0</mule.version>
		<mule.maven.plugin.version>4.1.1</mule.maven.plugin.version>
		<mule.tools.version>3.5.3</mule.tools.version>
		
		<!-- CloudHub Deployment Properties -->
		<cloudhub1.runtime.version>4.9.12</cloudhub1.runtime.version>
		<cloudhub2.runtime.version>4.11.1:5e</cloudhub2.runtime.version>
		
		<!-- Connector Versions -->
		<mule.http.connector.version>1.6.0</mule.http.connector.version>
		<mule.db.connector.version>1.10.4</mule.db.connector.version>
		<mule.email.connector.version>1.4.0</mule.email.connector.version>
		<mule.file.connector.version>1.3.4</mule.file.connector.version>
		<mule.apikit.version>1.6.1</mule.apikit.version>
		
		<!-- Database Versions -->
		<postgresql.version>42.7.2</postgresql.version>
		<h2.version>2.2.224</h2.version>
		
		<!-- Plugin Versions -->
		<maven.clean.plugin.version>3.2.0</maven.clean.plugin.version>
		<maven.compiler.plugin.version>3.11.0</maven.compiler.plugin.version>
	</properties>

	<build>
		<pluginManagement>
			<plugins>
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-clean-plugin</artifactId>
					<version>${maven.clean.plugin.version}</version>
					<configuration>
						<failOnError>false</failOnError>
						<retryOnError>true</retryOnError>
					</configuration>
				</plugin>
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-compiler-plugin</artifactId>
					<version>${maven.compiler.plugin.version}</version>
					<configuration>
						<source>17</source>
						<target>17</target>
						<compilerArgs>
							<arg>--add-exports=java.base/sun.nio.ch=ALL-UNNAMED</arg>
							<arg>--add-opens=java.base/java.lang=ALL-UNNAMED</arg>
							<arg>--add-opens=java.base/java.lang.reflect=ALL-UNNAMED</arg>
						</compilerArgs>
					</configuration>
				</plugin>
				<plugin>
					<groupId>org.mule.tools.maven</groupId>
					<artifactId>mule-maven-plugin</artifactId>
					<version>${mule.maven.plugin.version}</version>
					<extensions>true</extensions>
					<configuration>
						<!-- ✅ PRIMARY: CloudHub 1.0 with supported runtime -->
						<cloudHubDeployment>
							<uri>https://anypoint.mulesoft.com</uri>
							<muleVersion>${cloudhub1.runtime.version}</muleVersion>
							<environment>${anypoint.environment}</environment>
							<region>${anypoint.region}</region>
							<businessGroupId>${anypoint.organization_id}</businessGroupId>
							<connectedAppClientId>${anypoint.client_id}</connectedAppClientId>
							<connectedAppClientSecret>${anypoint.client_secret}</connectedAppClientSecret>
							<connectedAppGrantType>client_credentials</connectedAppGrantType>
							<workers>${anypoint.workers}</workers>
							<workerType>${anypoint.workerType}</workerType>
						</cloudHubDeployment>
						
						<!-- ✅ FALLBACK: CloudHub 2.0 -->
						<cloudHub2Deployment>
							<uri>https://anypoint.mulesoft.com</uri>
							<muleVersion>
								<version>${cloudhub2.runtime.version}</version>
							</muleVersion>
							<environment>${anypoint.environment}</environment>
							<region>${anypoint.region}</region>
							<businessGroupId>${anypoint.organization_id}</businessGroupId>
							<connectedAppClientId>${anypoint.client_id}</connectedAppClientId>
							<connectedAppClientSecret>${anypoint.client_secret}</connectedAppClientSecret>
							<connectedAppGrantType>client_credentials</connectedAppGrantType>
							<deploymentSettings>
								<vCoreCount>0.1</vCoreCount>
								<continuousDeployment>true</continuousDeployment>
							</deploymentSettings>
						</cloudHub2Deployment>
					</configuration>
				</plugin>
			</plugins>
		</pluginManagement>
		
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-clean-plugin</artifactId>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
			</plugin>
		</plugins>
	</build>

	<dependencyManagement>
		<dependencies>
			<!-- Mule Connectors -->
			<dependency>
				<groupId>org.mule.connectors</groupId>
				<artifactId>mule-http-connector</artifactId>
				<version>${mule.http.connector.version}</version>
				<classifier>mule-plugin</classifier>
			</dependency>
			<dependency>
				<groupId>org.mule.connectors</groupId>
				<artifactId>mule-db-connector</artifactId>
				<version>${mule.db.connector.version}</version>
				<classifier>mule-plugin</classifier>
			</dependency>
			<dependency>
				<groupId>org.mule.connectors</groupId>
				<artifactId>mule-email-connector</artifactId>
				<version>${mule.email.connector.version}</version>
				<classifier>mule-plugin</classifier>
			</dependency>
			<dependency>
				<groupId>org.mule.connectors</groupId>
				<artifactId>mule-file-connector</artifactId>
				<version>${mule.file.connector.version}</version>
				<classifier>mule-plugin</classifier>
			</dependency>
			<dependency>
				<groupId>org.mule.modules</groupId>
				<artifactId>mule-apikit-module</artifactId>
				<version>${mule.apikit.version}</version>
				<classifier>mule-plugin</classifier>
			</dependency>
			
			<!-- Database Dependencies -->
			<dependency>
				<groupId>org.postgresql</groupId>
				<artifactId>postgresql</artifactId>
				<version>${postgresql.version}</version>
			</dependency>
			<dependency>
				<groupId>com.h2database</groupId>
				<artifactId>h2</artifactId>
				<version>${h2.version}</version>
			</dependency>
		</dependencies>
	</dependencyManagement>

	<distributionManagement>
		<repository>
			<id>anypoint-exchange-v3</id>
			<name>Anypoint Exchange Repository</name>
			<url>https://maven.anypoint.mulesoft.com/api/v3/maven</url>
			<layout>default</layout>
		</repository>
	</distributionManagement>

	<repositories>
		<repository>
			<id>anypoint-exchange-v3</id>
			<name>Anypoint Exchange</name>
			<url>https://maven.anypoint.mulesoft.com/api/v3/maven</url>
			<layout>default</layout>
		</repository>
		<repository>
			<id>mulesoft-releases</id>
			<name>MuleSoft Releases Repository</name>
			<url>https://repository.mulesoft.org/releases/</url>
			<layout>default</layout>
		</repository>
	</repositories>

	<pluginRepositories>
		<pluginRepository>
			<id>mulesoft-releases</id>
			<name>MuleSoft Releases Repository</name>
			<url>https://repository.mulesoft.org/releases/</url>
			<layout>default</layout>
		</pluginRepository>
	</pluginRepositories>

</project>
