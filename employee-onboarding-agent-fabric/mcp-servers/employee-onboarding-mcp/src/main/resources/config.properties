# HTTP Listener Configuration
http.host=0.0.0.0
http.port=8081

# Database Configuration - Environment Specific
# PostgreSQL Configuration (Local Development)
db.postgres.url=jdbc:postgresql://localhost:5432/mcp_employees
db.postgres.username=postgres
db.postgres.password=postgres123
db.postgres.driverClassName=org.postgresql.Driver

# H2 Configuration (CloudHub Testing/Fallback)
db.h2.url=jdbc:h2:mem:mcp_employees;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;MODE=PostgreSQL
db.h2.username=sa
db.h2.password=
db.h2.driverClassName=org.h2.Driver



# MCP Server Configuration
mcp.serverName=Employee Onboarding MCP Server
mcp.serverVersion=1.0.0

# Environment Configuration
env=${env:development}

# \u2705 FIXED: CloudHub safe properties
cloudhub.worker.type=Micro
cloudhub.region=us-east-1

# \u2705 FIXED: Database init (CloudHub safe)
app.startup.database.init=${app.startup.database.init:false}

# \u2705 ADDED: Secure properties (CloudHub override)
db.postgres.url.prod=jdbc:postgresql://prod-db-host:5432/mcp_employees
db.username.prod=prod_employee_user
db.password.prod=${secure::prod_employee_pass}
